# template for server
app:
  host-url: http://localhost
spring:
  datasource:
    #    url: jdbc:postgresql://localhost:5432/jira
    # db - it`s docker compose container name
    url: jdbc:postgresql://db:5432/jira
    username: jira
    password: JiraRush
